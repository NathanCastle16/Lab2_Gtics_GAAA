# LAB2 GTICS 2026-2

Solución del laboratorio Spring Data JPA + Data Binding.

Antes de ejecutar: carga la BD `hr` en MySQL y ajusta la contraseña en `application.properties`.

Implementa P1-P5 y deja instrucciones de ramas para P6. El botón Eliminar solo se muestra, no se implementa, tal como pide el enunciado.
